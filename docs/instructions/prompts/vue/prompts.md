---
title: Vue 提示词集
---

# Vue 提示词集

面向 Vue 3 开发场景的 AI 提示词集合。每个 Prompt 用代码块包裹，**复制后替换 `[方括号]` 内容即可直接使用**。

---

## B. Vue 组件开发（日常高频）

## VB1. 新建基础组件

> 适用：创建一个标准 Vue 3 组件，含完整 TS 类型定义

```text
请先阅读 ./constraints.md 作为基础上下文。
编写 [组件名] 组件，技术栈：[组件库] + [样式方案]。
功能：
1. [功能点1，如：通过 v-model 绑定输入值]
2. [功能点2，如：点击外部自动关闭]
```

---

## VB2. 新建表单组件

> 适用：带校验、联动、错误提示的表单组件

```text
请先阅读 ./constraints.md 作为基础上下文。
编写 [表单名，如：用户注册] 表单组件，使用 [组件库]。
字段：
1. [字段1] - [校验规则]
2. [字段2] - [校验规则]
3. [字段3] - [校验规则，如：异步校验]
联动逻辑：当 [条件字段] 值为 [某值] 时，显示/隐藏 [目标字段]
```

---

## VB3. 新建列表/表格组件

> 适用：带分页、搜索、排序的数据列表

```text
请先阅读 ./constraints.md 作为基础上下文。
封装 [业务名，如：订单管理] 高级搜索列表组件，使用 [组件库]。
搜索栏：[N] 个筛选条件，支持展开/收起
表格列：序号、[字段1]、[字段2]、状态标签、操作列
```

---

## VB4. 新建弹窗/对话框组件

> 适用：可复用的弹窗组件，支持内容插槽和确认/取消

```text
请先阅读 ./constraints.md 作为基础上下文。
封装通用 [弹窗类型，如：确认删除/表单编辑/详情预览] 弹窗组件，使用 [组件库]。
支持 default 插槽自定义内容、loading 防重复点击、关闭时自动重置内部状态。
[可选] 支持 fullscreen 属性。
```

---

## VB5. 组件大文件拆解

> 适用：将超过 300 行的大组件拆分为模块化架构

```text
请先阅读 ./constraints.md 作为基础上下文。
组件超过 [N] 行，职责：[描述]，问题：[如：模板太长 / 逻辑视图耦合]。
按主组件 + 子组件 + composable + utils 架构拆解，给出目录结构和编排代码。
```

---

## VB6. 组件性能优化

> 适用：解决渲染卡顿、不必要的重绘等性能问题

```text
请先阅读 ./constraints.md 作为基础上下文。
组件存在性能问题：[如：v-for 卡顿 / 全量重绘 / 输入延迟]。
当前代码：
[粘贴代码]
请排查并给出优化方案。
```

---

## VB7. Composable 提取

> 适用：从组件中提取可复用逻辑为 useXxx 函数

```text
请先阅读 ./constraints.md 作为基础上下文。
将组件中 [需要提取的逻辑，如：数据请求和分页] 提取为 composable。
组件功能：[描述功能]
```

---

## VB8. 样式穿透与主题适配

> 适用：覆盖组件库样式 + 亮暗主题切换

```text
请先阅读 ./constraints.md 作为基础上下文。
覆盖 [组件库名] 的 [组件名] 样式，目标：[样式变化]，问题：[如：:deep() 不生效]。
给出穿透代码、CSS 变量主题方案和优先级控制建议。
```

---

## VB9. Pinia Store 设计

> 适用：设计状态管理模块

```text
请先阅读 ./constraints.md 作为基础上下文。
设计 Pinia Store 管理 [业务模块，如：用户信息/购物车]。
State：[字段列表]，Getters：[计算逻辑]，Actions：[异步请求]，持久化：[指定字段]。
```

---

## VB10. 路由与权限

> 适用：动态路由 + 按钮权限 + 面包屑

```text
请先阅读 ./constraints.md 作为基础上下文。
实现 RBAC 权限方案：动态路由挂载、v-permission 按钮指令、面包屑、路由守卫。
```

---

## VB11. ECharts 可视化封装

> 适用：封装通用图表组件

```text
请先阅读 ./constraints.md 作为基础上下文。
封装通用 BaseChart 组件，支持 [图表类型]，含 resize 自适应、点击事件 emit、内存泄漏防护。
```

---

## VB12. H5 移动端适配

> 适用：移动端列表、下拉刷新、图片懒加载

```text
请先阅读 ./constraints.md 作为基础上下文。
开发 H5 端 [业务名] 滚动列表，使用 [Vant / NutUI]。
含触底加载、下拉刷新、图片懒加载、px 转 vw 适配、iOS 兼容。
```

---

## C. JavaScript 通用

## VC1. 复杂数据结构处理

> 适用：数组/对象转换、统计、过滤、树结构

```text
请先阅读 ./constraints.md 作为基础上下文。
数据：[粘贴或描述结构]
转换：过滤 [条件] → 转换 [如：列表转树] → 统计 [字段] 的总和/平均值。
要求纯函数，含边界处理。
```

---

## VC2. 异步流程控制

> 适用：并发请求、串行请求、超时、竞态处理

```text
请先阅读 ./constraints.md 作为基础上下文。
场景：[如：并发 5 个接口 / 串行请求 / 搜索防竞态]。
要求：async/await + 错误处理 + 超时 + [竞态处理]。
```

---

## VC3. 工具函数封装

> 适用：防抖节流、深拷贝、日期格式化等通用工具

```text
请先阅读 ./constraints.md 作为基础上下文。
封装 [功能名] 工具函数，要求：高性能、健壮（[边界情况]）、兼容浏览器和 Node.js、含 TS 类型。
```

---

## VC4. DOM 操作与原生交互

> 适用：不依赖框架的原生 JS 实现

```text
请先阅读 ./constraints.md 作为基础上下文。
原生 JS 实现 [功能名]，使用最新 DOM API，集成防抖/节流，样式隔离，含 init/destroy 方法。
```

---

## D. Web/H5 应用

## VD1. H5 无限滚动列表

> 适用：移动端商品列表、信息流

```text
请先阅读 ./constraints.md 作为基础上下文。
开发 H5 端 [业务名] 无限滚动列表，使用 [Vant/NutUI]。
含触底加载、下拉刷新、图片懒加载、IntersectionObserver 优化、iOS 兼容。
```

---

## VD2. PC 官网响应式页面

> 适用：产品介绍、特性展示等官网页面

```text
请先阅读 ./constraints.md 作为基础上下文。
开发 [模块名] 响应式页面，使用 [Tailwind / UnoCSS]。
适配 1920/1440/平板，含滚动入场动画、hover 交互、语义化 SEO。
```

---

## VD3. H5 营销活动页

> 适用：倒计时、抢购、报名表等活动页面

```text
请先阅读 ./constraints.md 作为基础上下文。
H5 活动页 [倒计时/报名表] 组件，场景：[描述]。
requestAnimationFrame 精准计时、结束自动处理、埋点预留、px 转 vw 适配。
```

---

## VD4. 多端适配方案

> 适用：一套代码兼容 H5 和 PC

```text
请先阅读 ./constraints.md 作为基础上下文。
[功能名] 模块同时兼容 H5 和 PC。
给出 Media Queries 方案、useDevice composable、触摸/点击事件处理、组件拆分策略。
```
